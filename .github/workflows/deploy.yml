name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build
        env:
          # ── Polygon RPC ────────────────────────────────────────────────────
          VITE_POLYGON_RPC_URL: ${{ secrets.VITE_POLYGON_RPC_URL }}
          # ── Magic Embedded Wallet ──────────────────────────────────────────
          VITE_MAGIC_PUBLISHABLE_KEY: ${{ secrets.VITE_MAGIC_PUBLISHABLE_KEY }}
          # ── Polymarket Builder Program ─────────────────────────────────────
          # The builder SECRET must NEVER be set here — it belongs in
          # Vercel environment variables only (see README for details).
          VITE_POLYMARKET_BUILDER_API_KEY: ${{ secrets.VITE_POLYMARKET_BUILDER_API_KEY }}
          VITE_POLYMARKET_BUILDER_PASSPHRASE: ${{ secrets.VITE_POLYMARKET_BUILDER_PASSPHRASE }}
          VITE_POLYMARKET_BUILDER_RELAY_SIGN_URL: ${{ secrets.VITE_POLYMARKET_BUILDER_RELAY_SIGN_URL }}
          # ── Payment gateways ───────────────────────────────────────────────
          # PayPal client ID is browser-safe (public identifier).
          VITE_PAYPAL_CLIENT_ID: ${{ secrets.VITE_PAYPAL_CLIENT_ID }}
          VITE_COINBASE_CHECKOUT_ID: ${{ secrets.VITE_COINBASE_CHECKOUT_ID }}
          # NOTE: Payfast credentials (PAYFAST_MERCHANT_ID, PAYFAST_MERCHANT_KEY,
          # PAYFAST_PASSPHRASE, PAYFAST_ITN_SECRET, PAYFAST_SANDBOX) are server-side
          # only.  Set them in Vercel → Project Settings → Environment Variables.
          # Do NOT add them here.
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
